# Copy this to a file "secrets.yml"
# NEVER fill out secrets in this file.
# That makes them more likely to be committed.

# Add secrets next to each var

db_user: # postgres
db_password: # changeMe
db_name: # rarduino